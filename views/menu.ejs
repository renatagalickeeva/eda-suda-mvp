<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Меню - Еда-сюда</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .dish-item { border: 1px solid #ddd; padding: 15px; margin: 10px 0; border-radius: 5px; }
        button { background: #28a745; color: white; border: none; padding: 8px 15px; border-radius: 3px; cursor: pointer; }
        button:hover { background: #218838; }
        .nav { margin: 20px 0; }
        .nav a { margin-right: 15px; text-decoration: none; color: #007bff; }
    </style>
</head>
<body>
    <div class="nav">
        <a href="/">🏠 Главная</a>
        <a href="/cart">🛒 Корзина</a>
    </div>

    <h1> Наше меню</h1>
    
    <div id="menu">
        <% dishes.forEach(dish => { %>
            <div class="dish-item">
                <h3><%= dish.name %></h3>
                <p>💰 Цена: <%= dish.price %> руб.</p>
                <p>🏷️ Категория: <%= dish.category %></p>
                <button onclick="addToCart(<%= dish.id %>, '<%= dish.name %>', <%= dish.price %>)">
                    ➕ В корзину
                </button>
            </div>
        <% }); %>
    </div>

    <script>
        function addToCart(id, name, price) {
            let cart = JSON.parse(localStorage.getItem('cart')) || [];
            cart.push({ id, name, price });
            localStorage.setItem('cart', JSON.stringify(cart));
            alert(`"${name}" добавлено в корзину!`);
       
function viewCart() {
    window.location.href = '/cart';
}


function updateCartCounter() {
    const cart = JSON.parse(localStorage.getItem('cart')) || [];
    const counter = document.getElementById('cart-counter');
    if (counter) {
        counter.textContent = cart.length;
    }
}


document.addEventListener('DOMContentLoaded', function() {
    updateCartCounter();
});
        }
    </script>
</body>
</html>